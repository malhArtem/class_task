## 🎯 Задача: "Текстовая RPG-игра 'Мастер заклинаний'"

### 📝 Описание игры:
Вы - молодой маг, который изучает магические заклинания. Ваша цель - создавать и комбинировать заклинания, сражаться с монстрами и повышать свой уровень.

---

## 🧩 Часть 1: Система заклинаний

```python
# 📊 База данных заклинаний
SPELLS_DATABASE = {
    "fireball": {
        "name": "Огненный шар",
        "element": "fire", 
        "power": 25,
        "cost": 15,
        "level": 1
    },
    "ice_shard": {
        "name": "Ледяная осколка",
        "element": "ice",
        "power": 20,
        "cost": 12,
        "level": 1
    },
    "lightning": {
        "name": "Удар молнии", 
        "element": "lightning",
        "power": 30,
        "cost": 18,
        "level": 2
    }
}

# 🎭 Элементы и их взаимодействия
ELEMENT_BONUS = {
    "fire": {"ice": 1.5, "nature": 1.5, "water": 0.5},
    "ice": {"fire": 0.5, "lightning": 1.5, "earth": 1.5},
    "lightning": {"water": 1.5, "ice": 0.5, "air": 1.5}
}
```

### 🎯 Задания:

**1. Функция создания мага:**
```python
def create_wizard(name, element_specialization):
    """
    Создает профиль мага
    Возвращает: словарь с характеристиками
    """
    # Должна возвращать:
    # {
    #     "name": "Гендальф",
    #     "level": 1,
    #     "health": 100,
    #     "mana": 50,
    #     "experience": 0,
    #     "element": "fire",
    #     "known_spells": [],
    #     "inventory": {"gold": 50, "potions": 3}
    # }
```

**2. Функция изучения заклинания:**
```python
def learn_spell(wizard, spell_name):
    """
    Маг изучает новое заклинание, если хватает уровня
    Возвращает: bool (успех/неудача)
    """
```

**3. Функция кастования заклинания:**
```python
def cast_spell(wizard, target, spell_name):
    """
    Маг использует заклинание на цель
    Учитывает: уровень, ману, бонусы стихий
    Возвращает: урон и оставшуюся ману
    """
```

---

## 🐉 Часть 2: Система боя

```python
# 🎯 База монстров
MONSTERS = {
    "goblin": {"health": 60, "attack": 15, "element": "earth", "xp": 25},
    "ice_dragon": {"health": 150, "attack": 35, "element": "ice", "xp": 100},
    "fire_demon": {"health": 120, "attack": 40, "element": "fire", "xp": 80}
}
```

**4. Функция боя:**
```python
def start_battle(wizard, monster_type):
    """
    Симуляция боя с монстром
    Пошаговый бой до победы или поражения
    Возвращает: результат боя и полученный опыт
    """
```

**5. Функция расчета урона со стихийными бонусами:**
```python
def calculate_damage(spell_element, target_element, base_power):
    """
    Рассчитывает итоговый урон с учетом стихий
    fire > ice > lightning > water > fire
    Возвращает: модифицированный урон
    """
```

---

## 🔮 Часть 3: Комбинирование заклинаний

**6. Функция создания комбо-заклинаний:**
```python
def create_combo_spell(spell1, spell2, wizard_level):
    """
    Создает новое заклинание из двух существующих
    Пример: fireball + ice_shard = steam_explosion
    Возвращает: новое заклинание или None если комбинация невозможна
    """
```

**7. База комбинаций:**
```python
SPELL_COMBINATIONS = {
    ("fireball", "ice_shard"): "steam_explosion",
    ("lightning", "ice_shard"): "storm_call", 
    ("fireball", "lightning"): "plasma_orb"
}

COMBO_SPELLS = {
    "steam_explosion": {
        "name": "Паровой взрыв", 
        "element": "water",
        "power": 45,
        "cost": 25,
        "level": 3
    }
}
```

---

## 📊 Часть 4: Система прокачки и инвентаря

**8. Функция повышения уровня:**
```python
def level_up(wizard):
    """
    Проверяет, достиг ли маг нужного опыта для повышения уровня
    Увеличивает характеристики, открывает новые заклинания
    """
```

**9. Функция покупки в магической лавке:**
```python
def visit_magic_shop(wizard, item, cost):
    """
    Покупка зелий, свитков, новых заклинаний
    Возвращает: обновленный инвентарь
    """
```

---

## 🎮 Главная игровая петля

```python
def main_game_loop():
    """
    Основная функция игры с меню:
    1. Изучить заклинание
    2. Отправиться на битву  
    3. Создать комбо-заклинание
    4. Посетить магазин
    5. Посмотреть статистику
    6. Выйти
    """
    player = create_wizard(input("Введите имя мага: "), "fire")
    
    while True:
        print(f"\n=== Мастер заклинаний: {player['name']} ===")
        print(f"Уровень: {player['level']} | Здоровье: {player['health']} | Мана: {player['mana']}")
        print("1. Изучить заклинание")
        print("2. Сразиться с монстром")
        print("3. Создать комбо-заклинание")
        print("4. Магическая лавка")
        print("5. Статистика")
        print("6. Выход")
        
        choice = input("Выберите действие: ")
        # Реализация меню...
```

---

## 🧪 Пример использования:

```python
# Создаем мага
gandalf = create_wizard("Гендальф", "fire")

# Изучаем заклинание
learn_spell(gandalf, "fireball")

# Сражаемся с монстром
start_battle(gandalf, "goblin")

# Создаем комбо-заклинание
combo = create_combo_spell("fireball", "ice_shard", gandalf["level"])
```

