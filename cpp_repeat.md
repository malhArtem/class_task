### Задание 1. «Знакомство заново» (Ввод и вывод)
Напишите программу, которая:
1. Просит пользователя ввести его **имя** (одним словом, без пробелов).
2. Просит ввести **возраст** (целое число).
3. Выводит на экран фразу:  
   `Привет, [Имя]! Тебе [Возраст] лет. Через 5 лет тебе будет [Возраст + 5].`

**💡 Подсказка:**
```cpp
#include <iostream>
using namespace std;

int main() {
    string name;  // для имени
    int age;      // для возраста
    cout << "Введите имя: ";
    cin >> name;
    // ... дальше сами
    return 0;
}
```

---

### Задание 2. «Чёт или нечет?» (Условие if-else)
Программа просит пользователя ввести **одно целое число**. Нужно:
- Если число чётное — вывести `"Чётное"`.
- Если нечётное — вывести `"Нечётное"`.
- **Дополнительно:** если число делится на 5 без остатка, вывести ещё и `"Кратно 5"`.

**Пример работы:**
```
Введите число: 10
Чётное
Кратно 5
```

**💡 Подсказки:**
- Чётность проверяется так: `if (number % 2 == 0)`
- Кратность 5: `if (number % 5 == 0)`
- `%` — это оператор "остаток от деления".

---

### Задание 3. «Таблица умножения на 5» (Цикл for)
Программа с помощью цикла `for` выводит на экран таблицу умножения на 5 от 1 до 10 вот так:
```
5 * 1 = 5
5 * 2 = 10
5 * 3 = 15
...
5 * 10 = 50
```

**💡 Подсказка:**
```cpp
for (int i = 1; i <= 10; i++) {
    cout << "5 * " << i << " = " << 5 * i << endl;
}
```

---

### Задание 4. «Сумма всех чисел» (Цикл while)
Программа просит пользователя вводить целые числа до тех пор, пока он не введёт **ноль** (0). 
Как только введён 0 — программа завершается и выводит на экран **сумму всех введённых чисел** (ноль не считается).

**Пример работы:**
```
Введите число: 3
Введите число: 7
Введите число: 2
Введите число: 0
Сумма = 12
```

**💡 Подсказки:**
- Используйте цикл `while (true)` или `while (number != 0)`.
- Не забудьте завести переменную `int sum = 0;` и прибавлять к ней каждое введённое число (кроме нуля).

---

### Задание 5. «Угадай число» (Комбинация всего)
Программа "загадывает" число 7 (просто записано в коде). Пользователь должен его угадать.
- В цикле программа спрашивает: `"Введите число: "`.
- Если пользователь ввёл меньше 7 — вывести `"Больше!"`.
- Если больше 7 — вывести `"Меньше!"`.
- Если угадал (ввёл 7) — вывести `"Поздравляю!"` и завершить программу.

**💡 Подсказки:**
- Используйте цикл `while (true)`.
- Внутри цикла: `cin >> guess;`, потом три проверки `if` (меньше, больше, равно).
- Выход из цикла — `break;` когда угадал.

---
