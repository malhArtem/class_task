### Задание 1. «Знакомство заново» (Ввод и вывод)
Напишите программу, которая:
1. Просит пользователя ввести его **имя** (одним словом, без пробелов).
2. Просит ввести **возраст** (целое число).
3. Выводит на экран фразу:  
   `Привет, [Имя]! Тебе [Возраст] лет. Через 5 лет тебе будет [Возраст + 5].`

**Подсказка:**
```cpp
#include <iostream>
using namespace std;

int main() {
    string name;  // для имени
    int age;      // для возраста
    cout << "Введите имя: ";
    cin >> name;
    // ... дальше сами
    return 0;
}
```

---

### Задание 2. «Чёт или нечет?» (Условие if-else)
Программа просит пользователя ввести **одно целое число**. Нужно:
- Если число чётное — вывести `"Чётное"`.
- Если нечётное — вывести `"Нечётное"`.
- **Дополнительно:** если число делится на 5 без остатка, вывести ещё и `"Кратно 5"`.

**Пример работы:**
```
Введите число: 10
Чётное
Кратно 5
```

**Подсказки:**
- Чётность проверяется так: `if (number % 2 == 0)`
- Кратность 5: `if (number % 5 == 0)`
- `%` — это оператор "остаток от деления".

---

### Задание 3. «Таблица умножения на 5» (Цикл for)
Программа с помощью цикла `for` выводит на экран таблицу умножения на 5 от 1 до 10 вот так:
```
5 * 1 = 5
5 * 2 = 10
5 * 3 = 15
...
5 * 10 = 50
```

**Подсказка:**
```cpp
for (int i = 1; i <= 10; i++) {
    cout << "5 * " << i << " = " << 5 * i << endl;
}
```

---

### Задание 4. «Сумма всех чисел» (Цикл while)
Программа просит пользователя вводить целые числа до тех пор, пока он не введёт **ноль** (0). 
Как только введён 0 — программа завершается и выводит на экран **сумму всех введённых чисел** (ноль не считается).

**Пример работы:**
```
Введите число: 3
Введите число: 7
Введите число: 2
Введите число: 0
Сумма = 12
```

**Подсказки:**
- Используйте цикл `while (true)` или `while (number != 0)`.
- Не забудьте завести переменную `int sum = 0;` и прибавлять к ней каждое введённое число (кроме нуля).

---

### Задание 5. «Угадай число» (Комбинация всего)
Программа "загадывает" число 7 (просто записано в коде). Пользователь должен его угадать.
- В цикле программа спрашивает: `"Введите число: "`.
- Если пользователь ввёл меньше 7 — вывести `"Больше!"`.
- Если больше 7 — вывести `"Меньше!"`.
- Если угадал (ввёл 7) — вывести `"Поздравляю!"` и завершить программу.

**Подсказки:**
- Используйте цикл `while (true)`.
- Внутри цикла: `cin >> guess;`, потом три проверки `if` (меньше, больше, равно).

---

### Задание 6. «Прямоугольник из звёздочек» (Вложенные циклы)
Программа просит пользователя ввести **ширину** и **высоту** (целые числа). Затем рисует прямоугольник из символов `*`.

**Пример работы:**
```
Введите ширину: 5
Введите высоту: 3
*****
*****
*****
```

**Подсказки:**
- Внешний цикл — по строкам (`for (int i = 0; i < height; i++)`).
- Внутренний цикл — по столбцам (`for (int j = 0; j < width; j++)`).
- Внутри внутреннего цикла печатайте `*` через `cout << "*";` (без `endl`!).
- После внутреннего цикла — один `cout << endl;`, чтобы перейти на новую строку.

---

### Задание 7. «Считаем до N» (Цикл + накопление)
Пользователь вводит число **N**. Программа выводит:
1. Все числа от 1 до N.
2. Их сумму.
3. Их среднее арифметическое (как дробное число).

**Пример работы:**
```
Введите N: 5
Числа: 1 2 3 4 5
Сумма: 15
Среднее: 3
```

**Подсказки:**
- Заведите `int sum = 0;` перед циклом.
- Внутри цикла: `sum += i;` (это то же самое, что `sum = sum + i;`).
- Среднее = `(double)sum / N` — не забудьте привести к `double`, иначе получите целое число.

---

### Задание 8. «Максимум из трёх» (Условия)
Программа просит ввести **три целых числа**. Нужно вывести **наибольшее** из них.

**Пример работы:**
```
Введите три числа: 4 9 2
Максимум: 9
```

**Подсказки:**
- Заведите переменную `int max = a;` (пусть первое число будет "пока что максимумом").
- Затем сравните: если `b > max` — то `max = b;`. То же самое для `c`.
- В конце просто выведите `max`.

---

### Задание 9. «Обратный отсчёт» (Цикл for наоборот)
Программа просит ввести число **N**. Затем выводит все числа от N до 1, а в конце — `"Поехали!"`.

**Пример работы:**
```
Введите N: 5
5 4 3 2 1
Поехали!
```

**Подсказки:**
- Цикл `for` умеет идти в обратную сторону: `for (int i = N; i >= 1; i--)`.
- Обратите внимание на знак `>=` (не `>`), иначе не дойдёте до 1.

---

### Задание 10. «Средний балл студента» (Мини-комбинация)
Программа спрашивает у пользователя, **сколько оценок** он хочет ввести (например, 5). Затем в цикле запрашивает каждую оценку.

В конце программа выводит:
- Сумму всех оценок.
- Средний балл.
- Если средний балл **больше или равен 4.5** — вывести `"Отличник!"`.
- Если от 3.5 до 4.5 — `"Хорошист"`.
- Иначе — `"Троечник"`.

**Пример работы:**
```
Сколько оценок? 4
Оценка 1: 5
Оценка 2: 4
Оценка 3: 5
Оценка 4: 4
Сумма: 18
Средний балл: 4.5
Отличник!
```

**Подсказки:**
- Сначала спросите количество: `int count; cin >> count;`
- Затем цикл `for (int i = 0; i < count; i++)`, внутри — `cin >> grade;` и `sum += grade;`.
- Средний балл: `double avg = (double)sum / count;`
- Условия проверяйте по порядку: сначала `if (avg >= 4.5)`, потом `else if (avg >= 3.5)`, иначе `else`.

---

